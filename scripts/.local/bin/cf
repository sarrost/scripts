#! /bin/sh

# This script runs `fzf` on the ~/.config and ~/.scripts directories
# and opens the selected file in an nvim window.

du -a ~/.scripts ~/.config |  awk '{print $2}' | fzf | xargs -r $EDITOR
